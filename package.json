{
  "name": "static-web-experiments",
  "version": "0.1.0",
  "private": "true",
  "description": "An analytics module using TypeScript",
  "source": [
    "src/index.html"
  ],
  "browserslist": "> 0.5%, last 10 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "build:prod": "parcel build --public-url $PUBLIC_URL",
    "check": "tsc --noEmit"
  },
  "keywords": [
    "analytics",
    "typescript"
  ],
  "author": "T. Siva",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.8.3",
    "typescript": "^4.5.5"
  },
  "engines": {
    "node": ">= 14"
  }
}
